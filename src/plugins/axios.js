import axios from "axios"

// For multiple instances remove this file and the reference in main.js and use api/init.js

// For a single instance of axios you can configure the defaults
axios.defaults.baseURL = 'https://api.example.com'
